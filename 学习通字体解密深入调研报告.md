# 学习通（Chaoxing）模块与字体解密 深入调研报告

> **调研对象**：`yatori-go-console`（本仓库）及其核心依赖 `yatori-go-core v1.9.1-0.20260724131310-c9cddbeb4e4c`
> **版本核实**：该 pseudo-version 对应上游 yatori-go-core master HEAD `c9cddbe`（2026-07-24 提交），本地模块缓存与上游完全一致
> **调研日期**：2026-09-15
> **报告目的**：梳理学习通模块全貌，**重点剖析字体解密（字形哈希反查方案）的关键文件、关键代码、关键思路**，评估现状并给出补全路径，便于后续参考学习

---

## 目录

1. [架构总览：console 与 core 的分层](#1-架构总览)
2. [学习通模块功能全景](#2-学习通模块功能全景)
3. [背景：学习通字体反爬原理](#3-背景学习通字体反爬原理)
4. [字体解密实现深入剖析（核心章节）](#4-字体解密实现深入剖析)
5. [功能现状评估：五大问题证据链](#5-功能现状评估五大问题)
6. [Git 考古：功能演进时间线](#6-git-考古功能演进时间线)
7. [不解密的影响与当前兜底机制](#7-不解密的影响与当前兜底机制)
8. [社区方案对比](#8-社区方案对比)
9. [补全该功能的改造建议（代码级）](#9-补全该功能的改造建议)
10. [关键文件速查索引](#10-关键文件速查索引)
11. [术语表](#11-术语表)
12. [风险与合规提示](#12-风险与合规提示)

---

## 1. 架构总览

本仓库是 **Yatori 系列的控制台层（console）**，学习通的全部协议逆向、页面解析、加密对抗逻辑都在外部依赖 `yatori-go-core` 中。本仓库只做流程编排、配置管理与 Web UI。

```
┌────────────────────────────────────────────────────────────┐
│  yatori-go-console（本仓库）                                 │
│                                                            │
│  logic/xuexitong/XueXiTongPart.go    刷课流程编排（1275行）    │
│    ├─ UserLoginOperation    登录（密码/Cookie 双模式）         │
│    ├─ RunBrushOperation     多账号并发调度                     │
│    ├─ UserBlock/courseStudy 课程级流程（过滤/章测/作业/考试）     │
│    ├─ nodeRun               任务点分流（视频/文档/外链/直播/讨论） │
│    ├─ ExecuteVideo/Audio    学时提交循环（58s 周期+过超策略）     │
│    ├─ chapterTestAction     章测答题（AI/题库/内置AI 三通道）    │
│    └─ workAction/examAction 作业与考试答题                     │
│                                                            │
│  web/activity/XueXiTongActivity.go  Web 服务模式（同流程精简版）│
│  config/Config.go                   cx 系列学习通专属开关      │
│    CxNode(并发数) CxChapterTestSw(章测) CxWorkSw(作业)          │
│    CxExamSw(考试)                                             │
├────────────────────────────────────────────────────────────┤
│  yatori-go-core（核心依赖库，协议逆向都在这里）                  │
│                                                            │
│  aggregation/xuexitong/*   业务动作层                          │
│    ├─ XueXiTongCardAction.go      ★章测题目解析入口            │
│    │    ParseWorkQuestionAction（字体解密预定接入点）           │
│    ├─ XuXiTongWorkAction.go       作业题目解析                 │
│    ├─ XueXiTongExamAction.go      考试题目解析                 │
│    └─ XueXiTongLoginAction.go 等   登录/课程/章节/人脸          │
│                                                            │
│  api/xuexitong/*           裸 API 层（HTTP + HTML 解析）        │
│    ├─ XueXiTongChapterTestApi.go  ★★字体解密全部代码在此       │
│    ├─ XueXiTongCardApi.go         WorkFetchQuestion 题目拉取   │
│    └─ XueXiTongWorkApi.go 等                                     │
│                                                            │
│  utils/                                                 │
│    ├─ YatoriCoreInit.go           ★资产释放（映射表落盘）       │
│    └─ assets/                                          │
│        ├─ glyfHashed.json          ★★字形指纹库（30836条）      │
│        └─ cmap.json               ★★码点映射表（20902条）      │
│                                                            │
│  que-core/                 题库（localq）/ AI（aiq）/ 外挂题库   │
│  utils/qutils/             Levenshtein 相似度（乱码兜底）       │
└────────────────────────────────────────────────────────────┘
```

**依赖声明**（本仓库 [go.mod](go.mod#L20)）：

```go
github.com/yatori-dev/yatori-go-core v1.9.1-0.20260724131310-c9cddbeb4e4c //v2.0.5.0
```

---

## 2. 学习通模块功能全景

### 2.1 console 层（本仓库 [logic/xuexitong/XueXiTongPart.go](logic/xuexitong/XueXiTongPart.go)）

| 能力 | 位置 | 要点 |
|---|---|---|
| 登录 | `UserLoginOperation`（L43） | 密码长度 <50 走密码登录，≥50 视为 Cookie 串登录；支持 IP 代理池 |
| 多任务点并发 | `UserBlock`（L89） | VideoModel=3 时按 CxNode 数量复制会话并发刷（-1 无限制，有封号风险警告） |
| 视频刷课 | `ExecuteVideo`（L624） | 58 秒周期提交学时；mode 1=手机端/0=Web 端；过超提交阈值 `max(500s, 时长/2)`；`OutTimeMsg=="观看时长超过阈值"` 直接提交 |
| 人脸绕过 | L660-684 | 403 触发：`GetHistoryFaceImg`（拉历史人脸）→ `ImageRGBDisturb`（RGB 扰动）→ `PassFacePCAction`；"没有历史人脸/活体检测失败" 则引导用户放照片到 `assets/face/` |
| 答题三通道 | `chapterTestAction`（L980） | AutoExam：1=外接 AI 大模型 / 2=外挂题库 / 3=学习通内置 AI（`AnswerXXTAIGet`） |
| 提交策略 | L1139-1173 | ExamAutoSubmit：0=只保存 / 1=直接提交 / 2=`CheckAnswerIsAvoid` 检测留空后再决定 |
| 异常对抗 | 多处 | 500=风控跳过、403=人脸、404=重试、"章节未开放"=跳过、"已过时效/已截止"=跳过 |

### 2.2 core 层（题目拉取链路，字体加密即出现于此）

```
章测（章节小测验）:
  ChapterFetchCardsAction → ParsePointDto 分流 → workDTO
    → ParseWorkQuestionAction (aggregation/xuexitong/XueXiTongCardAction.go:334)
        → cache.WorkFetchQuestion()  (api/xuexitong/XueXiTongCardApi.go:434)
            GET https://mooc1-api.chaoxing.com/android/mworkspecial
            ?courseid&workid&jobid&knowledgeid&userid&clazzId&cpi&ktoken&enc...
            （手机端 UA）→ 返回题目 HTML ★题目文本可能被字体加密★
        → 若含 `<p class="blankTips">无效的权限,code=2</p>` → WorkFetch2Question 兜底
    → goquery 解析题干/选项（★此处 TODO：应先解密字体★）

作业:
  PullWorkListAction → EnterWorkAction → PullWorkQuestionAction
    → doHomeWork 手机端接口 → HtmlWorkQuestionTurnEntity 解析

考试:
  PullExamListAction → EnterExamAction → PullExamQuestionAction
    → XueXiTongExamApi.go 解析
```

---

## 3. 背景：学习通字体反爬原理

学习通在**章测/作业/考试页面**对题目文本（题干、选项）启用"**动态字体映射**"加密，本质是一种**所见非所存**的反爬技术：

```
┌────────────────────────────────────────────────────────────┐
│ 浏览器渲染层（用户看到的）                                      │
│     "下列哪项属于…"   ← 看起来完全正常                          │
├────────────────────────────────────────────────────────────┤
│ DOM / HTTP 响应层（爬虫拿到的）                                │
│     "\ue4a0\ue54b…"  ← 错误码点（PUA 私有区 / 生僻 CJK 字符）  │
├────────────────────────────────────────────────────────────┤
│ 加密字体（页面内嵌）                                           │
│     <style>@font-face{                                        │
│       font-family:'secretfont';                              │
│       src:url(data:font/font-ttf;base64,AAEAAA…)}            │
│     }</style>                                                 │
│     字体内部：码点 U+E4A0 → 字形"下"的轮廓                     │
│              码点 U+E54B → 字形"列"的轮廓                      │
│     （即：用错误的码点携带正确的字形）                           │
└────────────────────────────────────────────────────────────┘
```

**三个关键事实**：

1. 字体通常以 `data:font/font-ttf;base64,...` **内嵌在页面 HTML** 中（不走外链，增加抓取门槛）；
2. 映射关系**每次请求可变**，固定"乱码→明文"字典无法长期适用；
3. 加密字体的**字形名称（glyph name）通常被抹掉**，因此无法直接用"码点→字形名→字符"的 AGL 方式还原，必须走**字形轮廓比对/哈希反查**路线。

**不解密的后果**：题干/选项乱码 → AI 答题答非所问、外挂题库匹配失败、答案无法通过相似度映射回 A/B/C/D 选项字母。

---

## 4. 字体解密实现深入剖析

### 4.1 代码地图（一览）

| # | 文件 | 关键内容 | 行号 |
|---|---|---|---|
| A | `yatori-go-core/api/xuexitong/XueXiTongChapterTestApi.go` | **字体解密全部算法代码**（约160行） | L272-433 |
| B | `yatori-go-core/utils/assets/glyfHashed.json` | 字形指纹库（2.7MB / 30,836 条） | 整文件 |
| C | `yatori-go-core/utils/assets/cmap.json` | 字形名→码点映射（376KB / 20,902 条） | 整文件 |
| D | `yatori-go-core/utils/YatoriCoreInit.go` | `go:embed` 内嵌 + 运行时释放映射表到 `./assets/` | L17-69 |
| E | `yatori-go-core/aggregation/xuexitong/XueXiTongCardAction.go` | **预定接入点**：`ParseWorkQuestionAction` 中的 TODO 注释 | L332-356 |
| F | `yatori-go-core/api/xuexitong/XueXiTongCardApi.go` | `WorkFetchQuestion` 题目 HTML 拉取（解密的对象来源） | L434-509 |

> 说明：`yatori-go-core` 位于 Go module 缓存：
> `/root/go/pkg/mod/github.com/yatori-dev/yatori-go-core@v1.9.1-0.20260724131310-c9cddbeb4e4c/`

### 4.2 关键文件 A：`XueXiTongChapterTestApi.go`（核心算法）

文件开头是章测作业提交接口 `WorkNewSubmitAnswer`（L34-270），**L272 起是字体解密部分**，文件级注释：`//用于解析试卷加密字符的html`。

#### 4.2.1 全局变量与哈希函数（L275-297）

```go
var glyfHashed map[string]uint16   // ⚠ 声明与 JSON 实际结构不符（见 5.2）
var cmap map[string]rune           // ⚠ 同上

// keyFor：对一个字形的原始二进制数据做 SHA1+MD5 双哈希指纹
func keyFor(data []byte) string {
	h1 := sha1.Sum(data)
	h2 := md5.Sum(data)
	return hex.EncodeToString(h1[:]) + "|" + hex.EncodeToString(h2[:])
}

// loadJSONTables：从磁盘加载映射表（⚠ 路径问题见 5.3）
func loadJSONTables() error {
	gData, _ := os.ReadFile("glyfHashed.json")   // ⚠ 读的是工作目录根，而非 ./assets/
	cData, _ := os.ReadFile("cmap.json")
	if err := json.Unmarshal(gData, &glyfHashed); err != nil {
		return err
	}
	if err := json.Unmarshal(cData, &cmap); err != nil {
		return err
	}
	fmt.Println("✅ 字体映射表加载完成:", len(glyfHashed), "个哈希项")
	return nil
}
```

**思路**：`keyFor` 是整个方案的核心——**对 glyf 表中每个字形的原始字节流（glyph outline bytes）计算 `sha1|md5` 复合指纹**。双哈希拼接可以极大降低碰撞概率，且不依赖字形的任何元信息（名称、序号都可被平台篡改，唯独轮廓数据本身改了字形就变样）。

#### 4.2.2 TTF 二进制解析器（L301-363，纯手写、零第三方依赖）

```go
type tableRecord struct {
	Offset uint32
	Length uint32
}

type ttfFile struct {
	src    []byte
	tables map[string]tableRecord
}

// parseTTF：解析 sfnt 表目录
func parseTTF(b []byte) (*ttfFile, error) {
	r := bytes.NewReader(b)
	var numTables uint16
	if _, err := r.Seek(4, io.SeekStart); err != nil {  // 跳过 4 字节版本号
		return nil, err
	}
	binary.Read(r, binary.BigEndian, &numTables)          // 表数量
	r.Seek(6, io.SeekCurrent)                             // 跳过 searchRange/entrySelector/rangeShift
	tables := make(map[string]tableRecord)
	for i := 0; i < int(numTables); i++ {                 // 每张表 16 字节目录记录
		tag := make([]byte, 4)                            //   tag(4)
		r.Read(tag)
		r.Seek(4, io.SeekCurrent)                          //   checksum(4) 跳过
		var off, length uint32
		binary.Read(r, binary.BigEndian, &off)             //   offset(4)
		binary.Read(r, binary.BigEndian, &length)          //   length(4)
		tables[string(tag)] = tableRecord{Offset: off, Length: length}
	}
	return &ttfFile{src: b, tables: tables}, nil
}

func (t *ttfFile) table(tag string) ([]byte, error) { ... } // 按表名切出原始字节

// head 表第 50 字节：indexToLocFormat（0=短格式 loca，1=长格式）
func parseHeadIndexToLocFormat(head []byte) int16 {
	return int16(binary.BigEndian.Uint16(head[50:]))
}

// maxp 表第 4-6 字节：numGlyphs（字形总数）
func parseMaxpNumGlyphs(maxp []byte) uint16 {
	return binary.BigEndian.Uint16(maxp[4:6])
}

// loca 表 → 每个字形的字节偏移数组
//   长格式：uint32 直接读；短格式：uint16 读出后 ×2
func parseLoca(loca []byte, long bool, numGlyphs uint16) []uint32 {
	offsets := make([]uint32, numGlyphs+1)
	if long {
		for i := range offsets {
			offsets[i] = binary.BigEndian.Uint32(loca[i*4:])
		}
	} else {
		for i := range offsets {
			offsets[i] = uint32(binary.BigEndian.Uint16(loca[i*2:])) * 2
		}
	}
	return offsets
}
```

**思路**：只解析解密所必需的 4 张表（`head`/`maxp`/`loca`/`glyf`），跳过 checksum 校验，代码量最小化。TTF 知识补充见 4.6。

#### 4.2.3 `translate`：逐字形建映射（L366-403，算法核心）

```go
// translate: 计算哈希映射
func translate(font []byte) map[rune]rune {
	mapping := make(map[rune]rune)

	ttf, err := parseTTF(font)
	if err != nil { ... return mapping }

	head, _ := ttf.table("head")
	maxp, _ := ttf.table("maxp")
	loca, _ := ttf.table("loca")
	glyf, _ := ttf.table("glyf")

	locFormat := parseHeadIndexToLocFormat(head)
	numGlyphs := parseMaxpNumGlyphs(maxp)
	offsets := parseLoca(loca, locFormat != 0, numGlyphs)

	for i := 0; i < int(numGlyphs); i++ {
		start, end := offsets[i], offsets[i+1]
		if end <= start || int(end) > len(glyf) {   // 空字形/越界直接跳过
			continue
		}
		raw := glyf[start:end]                        // 第 i 个字形的原始轮廓字节
		k := keyFor(raw)                              // "sha1|md5" 复合指纹

		refGID, ok := glyfHashed[k]                   // ① 指纹 → 参考字形（⚠ 类型问题见 5.2）
		if !ok { continue }
		targetRune, ok := cmap[fmt.Sprint(refGID)]    // ② 参考字形 → 真实码点（⚠ 同上）
		if !ok { continue }
		mapping[rune(i)] = targetRune                 // ③ 字形序号 i → 真实字符
	}
	return mapping
}
```

**算法本质（三步反查）**：

```
加密字体中的字形 i
   │  取 glyf[start:end] 原始字节
   ▼
keyFor = sha1(字形字节) + "|" + md5(字形字节)
   │  查 glyfHashed.json（30836 条指纹库）
   ▼
参考字形名（如 "uni4E01"，即"丁"的字形）
   │  查 cmap.json（20902 条）
   ▼
真实 Unicode 码点（19969 = U+4E01 = "丁"）
   │
   ▼
mapping[字形i] = '丁'
```

即：**"这个字形长什么样"（哈希指纹）→ "它本来是谁"（参考字形名）→ "它该显示成什么字"（真实码点）**。加密字体可以把码点打乱、把名称抹掉，但只要字形轮廓数据和参考字体一致，指纹就能反查出真实身份。

#### 4.2.4 `decodeHTML`：入口函数（L407-433）

```go
func decodeHTML(html string) (string, error) {
	// ① 从 HTML 中提取内嵌的 base64 TTF 字体
	re := regexp.MustCompile(`data:(?:application|font)/font-ttf[^,]*,([A-Za-z0-9+/=]+)`)
	match := re.FindStringSubmatch(html)
	if len(match) == 0 {
		return "", errors.New("未检测到字体 base64")
	}
	fontBytes, _ := base64.StdEncoding.DecodeString(match[1])

	// ② 建立映射
	mapping := translate(fontBytes)

	// ③ goquery 遍历所有节点文本，逐 rune 替换
	doc, _ := goquery.NewDocumentFromReader(strings.NewReader(html))
	doc.Find("*").Each(func(_ int, s *goquery.Selection) {
		text := s.Text()
		var out strings.Builder
		for _, r := range text {
			if newRune, ok := mapping[r]; ok {
				out.WriteRune(newRune)
			} else {
				out.WriteRune(r)
			}
		}
		s.SetText(out.String())
	})
	return doc.Text(), nil   // ⚠ 返回 doc.Text() 拍平了 DOM 结构，见 5.5
}
```

**要点**：
- 正则 `data:(?:application|font)/font-ttf[^,]*,([A-Za-z0-9+/=]+)` 兼容 `font/font-ttf` 与 `application/font-ttf` 两种 MIME 写法，只匹配 **base64 内嵌**字体，不支持外链 `.ttf/.woff` URL；
- 替换逻辑是"**在 DOM 文本上原位替换 rune**"，未命中映射的字符原样保留（数字/英文/标点通常未加密）。

### 4.3 关键文件 B/C：两份映射表资产

通过 `go:embed` 打进二进制，运行时由 `YatoriCoreInit()` 释放到 `./assets/`。

**`glyfHashed.json`（字形指纹库，30,836 条，2.7MB）**

```json
{
  "ada6691afb1cfa64a8009a9013acf2891d9a84b7|6c9f21a73e828c82caeac41d8e2938a7": ".notdef",
  "d513fdac2194ba29811ef7d2cf1b9774b091ece3|e789f17561abefd6414f60206322abf9": "exclam",
  "51c7f9c5c7bf621a53bd916349a2ac9a03c673a6|fa2879a949ba59674cbdb00b1c319694": "quotedbl",
  "1044ed0b5f0ec51b35ea15b79cd7f845dfa5a9d3|72c2afa4197f52536e0a25e4c9f78a08": "numbersign"
}
```

- 键 = `sha1(glyf原始字节)|md5(glyf原始字节)`，与 `keyFor()` 输出格式严格对应；
- 值 = 参考字体里的**字形名**：ASCII 部分是 PostScript 名（`.notdef`/`exclam`/`numbersign`…），CJK 部分是 `uniXXXX` 形式；
- 覆盖 30,836 个字形（含 ASCII、常用/生僻 CJK、部首），基本是一个完整中文字库的规模。

**`cmap.json`（码点映射表，20,902 条，376KB）**

```json
{
  "uni2F00": 19968,   // 康熙部首"⼀" → U+4E00 "一"（视觉等价归一）
  "uni4E01": 19969,   // "丁" → U+4E01（自身码点）
  "uni4E02": 19970,
  "uni4E03": 19971
}
```

- 键 = 字形名（`uniXXXX`），值 = **真实 Unicode 码点**（十进制 int）；
- **设计亮点**：`uni2F00`（康熙部首，U+2F00）映射到 19968（U+4E00 "一"）——按**视觉等价**把部首/兼容字形归一到常用 CJK 码点；普通汉字 `uniXXXX` 则映射到自身码点。两表合并后：**任何"长得像某个字的字形"最终都能落到一个标准常用字**。

### 4.4 关键文件 D：`utils/YatoriCoreInit.go`（资产释放机制）

```go
//go:embed assets/tencentCollect.exe
//go:embed assets/tencentPowSolve.exe
//go:embed assets/node_modules/jsdom/**/*
//go:embed assets/cmap.json        // ★ 码点映射表
//go:embed assets/glyfHashed.json  // ★ 字形指纹库
var assets embed.FS

var assetsList = []string{
	"tencentCollect.exe", "tencentPowSolve.exe",
	"cmap.json", "glyfHashed.json",
}

func YatoriCoreInit() {
	loadAssets()        // 检查 ./assets/ 下文件是否齐全，缺则释放
	loadAiEnvironment() // ddddocr OCR 初始化
	loadNodeModules()   // jsdom 模块释放
	PathExistForCreate("./assets/faces")
}

func loadAssets() {
	for _, fileName := range assetsList {
		exists, _ := PathExists("./assets/" + fileName)
		if !exists {
			writeAssetsToDisk() // 从 embed.FS 写出到 ./assets/
			break
		}
	}
}
```

**调用链**：本仓库 [init/YatoriConsoleInit.go:19](init/YatoriConsoleInit.go#L19) 在控制台启动时调用 `utils.YatoriCoreInit()`，把映射表释放到**运行目录的 `./assets/`** 子目录。
**⚠ 注意**：而 `loadJSONTables()` 读的是**工作目录根**的 `glyfHashed.json`/`cmap.json` —— 两处路径不一致（见 5.3）。

### 4.5 关键文件 E：预定接入点（`XueXiTongCardAction.go:332-356`）

```go
// ParseWorkQuestionAction 用于解析作业题目，包括题目类型和题目文本
func ParseWorkQuestionAction(cache *xuexitong.XueXiTUserCache, workPoint *xuexitong.PointWorkDto) (xuexitong.Question, error) {
	...
	question, err := cache.WorkFetchQuestion(workPoint, 3, nil)   // 拉取题目 HTML
	if err != nil {
		// 若无权限则采用第二种方式
		if strings.Contains(err.Error(), `<p class="blankTips">无效的权限,code=2</p>`) {
			question, err = cache.WorkFetch2Question(workPoint, 3, nil)
		}
	}
	//先检测是否含有加密字体，如果有则先解密      ★★★ 预定接入点（TODO，L353）

	// 使用 goquery 解析 HTML               ★ 直接开始解析，解密步骤缺失
	doc, err := goquery.NewDocumentFromReader(bytes.NewReader([]byte(question)))
	...
	// 后续：ParseWorkInform 提取回填参数
	//       ParseQuestionSets 按 div.Py-mian1 切题
	//       按 [单选题]/[多选题]/[判断题]/[填空题]… 分类解析题干与选项
```

console 侧调用处：[XueXiTongPart.go:455](logic/xuexitong/XueXiTongPart.go#L455)
`questionAction, err2 := xuexitong.ParseWorkQuestionAction(userCache, &workDTO)`。

### 4.6 TTF 二进制结构知识补充（读懂 4.2.2/4.2.3 的前置知识）

```
TTF (sfnt) 文件布局（全部大端序）：

Offset  Size  字段
0       4     sfnt 版本号（TTF=0x00010000）
4       2     numTables        ← parseTTF 读这里
6       6     searchRange/entrySelector/rangeShift（二分查找辅助，直接跳过）
12      16×N  表目录数组，每条记录：
              ├─ tag(4)      表名，如 "head" "maxp" "loca" "glyf" "cmap"
              ├─ checksum(4) 本表校验和（本实现跳过）
              ├─ offset(4)   表数据在文件中的起始偏移
              └─ length(4)   表数据长度

解密用到的四张表：
┌────────┬──────────────────────────────────────────────┐
│ head   │ 全局头信息。offset 50 处的 indexToLocFormat：    │
│        │   0 = loca 用 uint16 存储（实际偏移=值×2）        │
│        │   1 = loca 用 uint32 存储                        │
├────────┼──────────────────────────────────────────────┤
│ maxp   │ offset 4 处的 numGlyphs = 字形总数               │
├────────┼──────────────────────────────────────────────┤
│ loca   │ 长度 (numGlyphs+1) 的偏移数组：字形 i 的数据在    │
│        │ glyf[ offsets[i], offsets[i+1] ) 区间           │
├────────┼──────────────────────────────────────────────┤
│ glyf   │ 所有字形的轮廓数据（点坐标/曲线指令）按序排列。      │
│        │ ★ 字形相等的充要证据：这段字节完全一致 ★           │
├────────┼──────────────────────────────────────────────┤
│ cmap   │ 码点 → 字形序号 的映射（浏览器靠它渲染文本）。      │
│        │ ⚠ 本实现未解析加密字体的 cmap（见 5.4）           │
└────────┴──────────────────────────────────────────────┘
```

**为什么"原始 glyf 字节"可以做指纹**：同一参考字体产出的加密字体，只是把字形重新分配到不同码点（改 cmap），字形轮廓数据本身不变（甚至字节级相同）。所以"对 glyf 原始字节做哈希"等价于"对字形本身做身份认证"。

### 4.7 完整解密流程（含缺口标注）

```
章测/作业页面 HTML（WorkFetchQuestion 拉回）
        │
        ▼
[✅已实现] ① decodeHTML 正则提取 @font-face 中的 base64 TTF
        │     （无字体 → 走正常解析路径，decodeHTML 返回 error）
        ▼
[✅已实现] ② parseTTF 解析 sfnt 表目录 → 取 head/maxp/loca/glyf 四表
        ▼
[✅已实现] ③ 逐字形取 glyf 原始字节 → keyFor 双哈希
        │     → 查 glyfHashed.json（3万条）→ 字形名
        │     → 查 cmap.json（2万条）→ 真实码点
        │     → mapping[字形序号] = 真实字符
        ▼
[❌缺失] ④ 解析加密字体自身 cmap：HTML文本码点 → 字形序号
        │     （当前 mapping 的键是"字形序号"，decodeHTML 却拿
        │       HTML 文本里的"码点"去查——两者并非同一概念）
        ▼
[❌缺失] ⑤ 修正 JSON 装载（类型/路径，见第 5 章）
        ▼
[❌缺失] ⑥ 在 ParseWorkQuestionAction L353 的 TODO 处接线：
        │     解密题干/选项 → 再进入 goquery 题目解析 → AI/题库作答
        ▼
   题目明文 → 三通道答题 → WorkNewSubmitAnswer 提交
```

---

## 5. 功能现状评估：五大问题

**总体结论：字体解密是"已完成地基、未完成装修"的半成品（约 80% 完成度），生产链路实际未启用。**

### 5.1 问题一：死代码（零调用）

全库验证（core + console 两仓库 grep）：`decodeHTML`、`translate`、`loadJSONTables` **没有任何调用点**。`loadJSONTables` 从未被调用意味着 `glyfHashed`/`cmap` 两个全局 map 永远为 nil，即使强行调用 `translate` 也只会得到空映射。

### 5.2 问题二：JSON 结构与代码声明不匹配（调用必崩）

实测两份 JSON 的真实结构（python 解析验证）：

| JSON | 实际键 | 实际值 | 代码声明 | 冲突 |
|---|---|---|---|---|
| glyfHashed.json | `"sha1hex\|md5hex"`（str） | **字形名字符串**（`.notdef`/`uni4E01`） | `map[string]uint16` | `".notdef"` 无法 Unmarshal 进 uint16 → **报错** |
| cmap.json | `"uniXXXX"`（str） | **int 码点**（19968） | `map[string]rune`，且按 `fmt.Sprint(refGID)`（字形**序号**）查询 | 键应为"uniXXXX"名称而非序号 → **永远查不到** |

即：若接通调用链，`loadJSONTables` 第一个 `json.Unmarshal` 就会失败。正确声明应为：

```go
var glyfHashed map[string]string  // "sha1|md5" → 字形名
var cmapTable map[string]int      // "uniXXXX" → 真实码点
```

且 `translate()` 内两处查询应改为 `name := glyfHashed[k]` → `code := cmapTable[name]`。

### 5.3 问题三：文件路径不一致

- `YatoriCoreInit()`（utils/YatoriCoreInit.go）把映射表释放到 **`./assets/`** 子目录；
- `loadJSONTables()`（XueXiTongChapterTestApi.go:287-288）从**工作目录根**读 `glyfHashed.json`/`cmap.json`。
两处不对齐，按当前代码即使修好类型也读不到文件。

### 5.4 问题四：映射键的语义缺口（最深的算法问题）

`translate()` 建立的映射是 **`字形序号 i → 真实字符`**：

```go
mapping[rune(i)] = targetRune   // i = 0..numGlyphs-1 的字形序号
```

而 `decodeHTML()` 拿 **HTML 文本中的字符码点** 去查这个映射：

```go
for _, r := range text {          // r 是页面文本里的码点（如 PUA U+E4A0）
    if newRune, ok := mapping[r]; ok { ... }
}
```

**"HTML 文本码点 → 字形序号"这一层转换依赖加密字体自身的 cmap 表，但代码从未解析它。** 当前实现只有在"加密字体的 cmap 恰好是恒等映射（码点数值==字形序号）"这一特殊假设下才碰巧正确。HTML 里的加密字符通常是 PUA 区（U+E000-F8FF）或生僻 CJK，与 0..30836 的字形序号数值域并不同——这极可能是作者尚未在真实加密页面上联调的原因。

正确做法（补丁思路）：

```
解析加密字体 cmap 表（format 4）得到 codeToGlyph: {码点 → 字形序号}
最终映射 = for (cp, gid) in codeToGlyph:
              mapping[chr(cp)] = 字形哈希反查结果[gid]
```

### 5.5 问题五：工程细节缺陷

- `decodeHTML` 末尾 `return doc.Text()` 把整棵 DOM 拍平成纯文本，丢失节点结构——对"解密后继续用 goquery 解析题目"的用途不友好（作为调试日志倒是可以）。另外 `doc.Find("*").Each` 对父子节点先后 SetText 的写法在嵌套结构下行为微妙（父节点 SetText 会移除子节点，后续对已脱离 DOM 的子节点操作无效，但父节点文本已含替换结果，功能上可接受，不够优雅）；
- 正则只覆盖 base64 内嵌字体，不支持外链 `.ttf/.woff` URL（部分页面/版本场景会走外链）；
- core 库 examples/ 下 16 个学习通页面样例 HTML（`学习通章测题目*.html`、`学习通考试试题页面.html` 等）**均不含加密字体**（只出现 `fontLabel` CSS 类名和普通 `font-family:宋体`），说明作者尚未采集带真实加密字体的页面做回归验证；
- 双哈希指纹库 3 万条是**静态资产**：chaoxing 一旦更换参考字体库（社区脚本历史上发生过，导致旧哈希批量失配），需要重新生成 `glyfHashed.json`。

---

## 6. Git 考古：功能演进时间线

通过克隆上游仓库并 `git log -S` 追踪 `decodeHTML`/`glyfHashed` 的增删历史：

| 时间 | 提交 | 标题 | 内容 |
|---|---|---|---|
| **2025-11-05** | `a69d446` | update: 优化学习通章结点状态 | **首次引入**字体解密：约 160 行追加在 `api/xuexitong/XueXiTongWorkApi.go` 尾部；**同时首次添加** `utils/assets/cmap.json`、`glyfHashed.json` 两份资产及 `YatoriCoreInit` 的释放逻辑（`+4` 行改动） |
| 2025-12-16 | `eebab09` | update: 添加支持学习通作业 | 作业模块大重构（新增 `XuXiTongWorkAction.go` 636 行等） |
| **2025-12-16** | `68886ee` | update: 添加支持学习通作业 | 字体解密代码块**整体迁移**至新文件 `api/xuexitong/XueXiTongChapterTestApi.go`（与新版作业提交接口 `WorkNewSubmitAnswer` 同文件，+434 行） |
| 2026-07-24 | `c9cddbe` | fix: 限制学习通验证码重试并保留失败原因 | 当前 master HEAD（= 本仓库 go.mod 锁定版本），字体解密代码无进一步改动 |

**解读**：作者在 2025-11-05 一次性放入了"算法 + 资产"，两个月后随作业模块重构挪了位置，此后 8 个月无实质推进——印证了 4.5 节 TODO 的判断：**感知到了反爬、完成了技术储备、尚未完成集成**。

---

## 7. 不解密的影响与当前兜底机制

生产链路（当前状态）题目乱码时的实际行为：

1. **题干乱码 → AI 答题质量不可控**：`AIProblemMessage` 直接把乱码题干发给 AI，答非所问；
2. **外挂题库匹配失败**：`AnswerExternalGet` 按题干文本查库，乱码查不到；
3. **答案无法映射回选项**：`SimilarityArraySelect`（Levenshtein 相似度，[qutils.go](../root/go/pkg/mod/github.com/yatori-dev/yatori-go-core@v1.9.1-0.20260724131310-c9cddbeb4e4c/utils/qutils/qutils.go)）在乱码选项上失配；
4. **兜底防线**：
   - `CheckAnswerIsAvoid`（console [XueXiTongPart.go:570](logic/xuexitong/XueXiTongPart.go#L570)）：选项与答案相似度 <0.9 判定"留空"，留空时**只保存不提交**，避免乱答送分；
   - 相似度阈值 0.9 的模糊匹配能容忍**少量**乱码字符（如个别字被加密），但无法应对整段加密。

---

## 8. 社区方案对比

| 方案 | 代表实现 | 原理 | 对平台改动的鲁棒性 | 备注 |
|---|---|---|---|---|
| **字形轮廓哈希反查** | **本仓库** | sha1+md5(原始 glyf 字节) → 预建指纹库 | 中：参考字体库更新则指纹库需重建 | 不依赖字形名，chaoxing 抹名也能工作 |
| 字形路径哈希 | ScriptCat 油猴脚本（Typr.js + Table.json） | `md5(JSON.stringify(glyphPath)).slice(24)` | 中：**该脚本记载 chaoxing 更新字体库后旧哈希全失效，靠 .slice(24) 修复**——印证指纹库维护是长期成本 | 纯前端 JS 生态 |
| cmap+AGL 直读 | fontTools `agl.toUnicode` | 加密字体保留字形名时：码点→字形名→字符 | 低：抹名即失效 | 学习通场景基本不可用（会抹名） |
| 字形轮廓相似度比对 | 早期爬虫方案 | 与标准字体逐点比对 xMin/xMax/y/…on | 高 | 计算量大、实现复杂 |
| OCR 渲染识别 | 截图/无头浏览器渲染后 OCR | 渲染出图→OCR 回文本 | 高 | 重量级，脱离了纯协议逆向路线 |

**本仓库方案（双哈希指纹反查）与主流一致且较优**：不依赖名称、不需要渲染、纯二进制解析、双哈希抗碰撞。核心短板不在算法而在"未接线 + 资产维护机制缺失"。

---

## 9. 补全该功能的改造建议

按优先级排列（均在 `yatori-go-core` 上游仓库修改）：

**① 修正映射表类型与查询逻辑**（`XueXiTongChapterTestApi.go`）

```go
var glyfHashed map[string]string // "sha1|md5" → 字形名
var cmapTable  map[string]int    // "uniXXXX" → 真实码点

var tablesOnce sync.Once
func loadJSONTables() error {    // 惰性加载 + 只读 ./assets/
	var loadErr error
	tablesOnce.Do(func() {
		gData, err1 := os.ReadFile("assets/glyfHashed.json")
		cData, err2 := os.ReadFile("assets/cmap.json")
		if err1 != nil || err2 != nil { loadErr = errors.Join(err1, err2); return }
		if err := json.Unmarshal(gData, &glyfHashed); err != nil { loadErr = err; return }
		if err := json.Unmarshal(cData, &cmapTable);  err != nil { loadErr = err; return }
	})
	return loadErr
}
```

`translate()` 内两处查询相应改为：

```go
name, ok := glyfHashed[k]
if !ok { continue }
code, ok := cmapTable[name]
if !ok { continue }
mapping[rune(i)] = rune(code)
```

**② 补充加密字体 cmap 解析**（修复 5.4 的语义缺口）

解析 sfnt `cmap` 表（`platformID=3/encodingID=1` 的 format 4 子表）得到 `码点→字形序号`，最终映射以**码点**为键：

```go
// 伪码：cmap format 4（分段映射）解析
// segCount = uint16(cmap子表+6)
// 1) endCode[]  2) reservedPad  3) startCode[]
// 4) idDelta[] 5) idRangeOffset[] 6) glyphIdArray[]
// 对每段: for cp in [startCode..endCode]:
//     gid = idRangeOffset==0 ? (cp+idDelta)&0xFFFF
//                           : glyphIdArray[...]
glyphToRune := translate(font)                    // 字形序号 → 真实字符
final := make(map[rune]rune)
for cp, gid := range parseCmap(ttf) {             // 码点 → 字形序号
    if r, ok := glyphToRune[rune(gid)]; ok {
        final[cp] = r                            // 码点 → 真实字符 ✅
    }
}
```

**③ 在预定接入点接线**（`XueXiTongCardAction.go:353` 的 TODO 处）

```go
question, err := cache.WorkFetchQuestion(workPoint, 3, nil)
...
//先检测是否含有加密字体，如果有则先解密
if strings.Contains(question, "font-ttf") {
    if decoded, derr := decodeHTML(question); derr == nil {
        question = decoded
    }
}
doc, err := goquery.NewDocumentFromReader(bytes.NewReader([]byte(question)))
```

（作业流 `HtmlWorkQuestionTurnEntity`、考试流解析入口做同样处理。）

**④ `decodeHTML` 保留 DOM 结构**：不要 `doc.Text()` 收尾，改为返回 `doc`（或序列化的 HTML），供后续 goquery 题目解析继续使用节点结构；替换循环建议只处理文本节点（`contents().FilterFunction(textNode)`），避免 SetText 级联副作用。

**⑤ 正则扩展**支持外链字体：匹配 `url(https?://[^)]+\.(?:ttf|woff2?))` 并下载后走同一 `translate` 流程。

**⑥ 建立回归样例**：在 core 的 `examples/` 补一个**含真实加密字体**的章测页面快照（当前 16 个样例均无加密字体），仿照 `card_entity_test.go` 写单测：`decodeHTML(样例HTML)` 断言输出明文。

**⑦ 指纹库维护机制**（长期）：提供 `glyfHashed.json` 的生成/更新脚本（用 fontTools 遍历参考字体导出 hash→name），当 chaoxing 更换字体库时可快速重建。

---

## 10. 关键文件速查索引

| 主题 | 文件 | 行号/说明 |
|---|---|---|
| ★★ 字体解密全部算法 | `yatori-go-core/api/xuexitong/XueXiTongChapterTestApi.go` | L272-433；`keyFor`(L280) `loadJSONTables`(L286) `parseTTF`(L311) `parseLoca`(L351) `translate`(L366) `decodeHTML`(L407) |
| ★★ 字形指纹库 | `yatori-go-core/utils/assets/glyfHashed.json` | 30,836 条：`"sha1\|md5" → 字形名` |
| ★★ 码点映射表 | `yatori-go-core/utils/assets/cmap.json` | 20,902 条：`"uniXXXX" → 码点int`（含部首视觉归一） |
| ★ 资产释放 | `yatori-go-core/utils/YatoriCoreInit.go` | L17-31 embed；L44-69 释放到 `./assets/` |
| ★ 预定接入点 TODO | `yatori-go-core/aggregation/xuexitong/XueXiTongCardAction.go` | L353 `//先检测是否含有加密字体，如果有则先解密` |
| 题目 HTML 拉取 | `yatori-go-core/api/xuexitong/XueXiTongCardApi.go` | L434 `WorkFetchQuestion`（android/mworkspecial 接口）；L590 `WorkFetch2Question` 兜底 |
| 作业题目解析 | `yatori-go-core/aggregation/xuexitong/XuXiTongWorkAction.go` | L331 `HtmlWorkQuestionTurnEntity`；L446 `extractWorkTitle`（`fontLabel` 选择器） |
| console 编排层 | 本仓库 `logic/xuexitong/XueXiTongPart.go` | L455 调用 `ParseWorkQuestionAction`；L980 章测答题；L1139 提交策略 |
| 留空检测兜底 | 本仓库 `logic/xuexitong/XueXiTongPart.go` | L570 `CheckAnswerIsAvoid`（相似度 0.9 阈值） |
| 相似度工具 | `yatori-go-core/utils/qutils/qutils.go` | Levenshtein + `SimilarityArraySelect`（答案→选项字母映射） |
| 依赖版本声明 | 本仓库 `go.mod` | L20 `yatori-go-core v1.9.1-0.20260724131310-c9cddbeb4e4c` |
| 启动初始化 | 本仓库 `init/YatoriConsoleInit.go` | L19 调用 `utils.YatoriCoreInit()` |

> `yatori-go-core` 本地路径：`/root/go/pkg/mod/github.com/yatori-dev/yatori-go-core@v1.9.1-0.20260724131310-c9cddbeb4e4c/`
> （调研时的可读克隆：`/tmp/yatori-go-core`，与上述版本同源）

---

## 11. 术语表

| 术语 | 含义 |
|---|---|
| 字体加密/字体反爬 | 页面文本用错误码点存储、由自定义字体渲染成正确字形，导致"所见非所存"的反爬技术 |
| PUA | Private Use Area，Unicode 私有使用区（U+E000–U+F8FF），常用作加密码点载体 |
| sfnt | TrueType/OpenType 的字体容器格式，由多张带 tag 的表组成 |
| cmap（字体表） | 字体内"码点 → 字形序号"的映射表，浏览器渲染的依据；**注意与项目里的 cmap.json（资产文件）区分** |
| glyf | 字形轮廓数据表，字形外观的最终事实来源 |
| loca | 字形偏移索引表，配合 glyf 切分每个字形的字节区间 |
| head / maxp | 全局头表（含 indexToLocFormat）/ 最大值表（含 numGlyphs） |
| 字形序号（GID） | 字体内部字形的编号，与 Unicode 码点是两个概念 |
| AGL | Adobe Glyph List，字形名（如 `uni4E01`/`exclam`）与 Unicode 的官方对照表 |
| 指纹反查 | 对字形原始字节做哈希，在预建参考指纹库中查身份的方案 |
| base64 内嵌字体 | `@font-face src:url(data:font/font-ttf;base64,...)` 形式，字体直接嵌在 HTML 里 |

---

## 12. 风险与合规提示

本项目属于自动化学习工具（刷课/代答），代码中可见学习通侧已部署的多层对抗：人脸抽查（403 触发绕过逻辑）、验证码风控（滑块 + 图形码 OCR 重试）、观看时长阈值、`OutTimeMsg` 风控、500 风控码处理等。**字体解密属于反爬对抗技术，本报告仅用于协议逆向原理的学习参考**。仓库作者在 README 中亦声明"程序只供技术学习使用，严禁贩卖、严禁滥用"。

---

## 附：核心结论一句话摘要

> 学习通字体解密在本项目中 = **"纯手写的 TTF 四表解析器 + SHA1/MD5 双哈希字形指纹 + 3 万条参考指纹库（glyfHashed.json→cmap.json 两级反查）"**，算法路线正确且与社区主流一致；但**当前为未接线的半成品**——存在死代码、JSON 类型/路径不匹配、缺加密字体 cmap 解析、`ParseWorkQuestionAction` 处仅一句 TODO 四类缺口，生产链路题目文本实际未解密，仅靠"留空不提交 + 相似度模糊匹配"降低乱答损失。补全路径：修类型（map[string]string/map[string]int）→ 修路径（./assets/）→ 补 cmap 码点转换 → 在 TODO 处接线 → 建加密页面回归样例。
